# Allowlist form: ignore everything in .beads/, then re-include the shared
# source-of-truth files. New volatile files br adds are ignored automatically.
*
!issues.jsonl
!config.yaml
!routes.jsonl
!.gitignore
